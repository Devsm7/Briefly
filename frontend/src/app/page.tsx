// Root page — redirect authenticated users to /dashboard, others to /login

// TODO: Import redirect from next/navigation
// TODO: Check auth token (cookie or localStorage via server component)
// TODO: redirect("/dashboard") if authenticated
// TODO: redirect("/login") if not

export default function HomePage() {
    // TODO: implement redirect logic
    return null;
}
