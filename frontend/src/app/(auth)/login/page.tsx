"use client";

// Login page — email + password form, link to register

// TODO: Import useAuth hook
// TODO: Import Input, Button UI components
// TODO: Import Link from next/link

export default function LoginPage() {
    // TODO: useAuth() → { login, isLoading, error }
    // TODO: Form state: email, password
    // TODO: handleSubmit → call login(email, password) → redirect to /dashboard
    //       or /onboarding if survey not completed

    return (
        <main>
            {/* TODO: Render sign-in form with dark glassmorphism card */}
            {/* TODO: Email input */}
            {/* TODO: Password input */}
            {/* TODO: Submit button */}
            {/* TODO: Link to /register */}
            {/* TODO: Show error message on failure */}
        </main>
    );
}
